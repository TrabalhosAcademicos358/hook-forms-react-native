import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
      height: 40,
      borderColor: '#CCCCCC',
      borderWidth: 1,
      borderRadius: 4,
      paddingLeft: 10,
      paddingRight: 10,
      fontSize: 16,
    },
});